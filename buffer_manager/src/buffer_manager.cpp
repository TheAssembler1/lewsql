#include "buffer_manager.h"

template<typename T>
uint8_t& BufferManager<T>::operator[](size_t cursor) {
  return 0;
}

template<typename T>
const uint8_t& BufferManager<T>::operator[](size_t cursor) const {
  return 0;
}
